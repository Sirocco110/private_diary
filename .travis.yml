sudo: required

services: docker

before_install:
  - docker-compose up --build -d

script:
  - docker-compose exec python3 manage.py migrate --settings private_diary.settings_dev
  - docker-compose exec python3 manage.py test --settings private_diary.settings_dev